### 问题分析
1. **雷达图不显示的根本原因**：
   - `drawRadarChart`函数在utils.js中是全局函数，但test.html中却以`utils.drawRadarChart()`的方式调用
   - 函数调用方式不匹配，导致函数无法找到，无法执行绘图操作
   - 存在冗余的`drawRadarChart`函数定义，造成代码混乱

2. **具体代码问题**：
   - `showResult`函数中调用`utils.drawRadarChart(totalScores, 'radar-chart')`
   - 但`drawRadarChart`是全局函数，不是`utils`对象的方法
   - test.html中存在冗余的`drawRadarChart`函数，递归调用导致错误

3. **修复方案**：
   - 移除test.html中冗余的`drawRadarChart`函数
   - 将`showResult`函数中的`utils.drawRadarChart()`调用改为直接调用全局函数

### 修复方案
1. **移除test.html中冗余的`drawRadarChart`函数**：
   - 删除第1157-1161行的`drawRadarChart`函数定义
   - 避免递归调用和代码混乱

2. **修复`showResult`函数中的函数调用**：
   - 将`utils.drawRadarChart(totalScores, 'radar-chart')`改为直接调用`drawRadarChart(totalScores, 'radar-chart')`
   - 确保函数调用方式与定义方式匹配

3. **确保Chart.js正确加载**：
   - 验证Chart.js库已正确引入
   - 确保canvas元素存在且可访问

### 预期效果
- 雷达图能正确绘制，显示四维度得分
- 无JavaScript错误
- 代码结构清晰，无冗余

### 文件修改
- 修改test.html，移除冗余函数并修复函数调用

### 测试步骤
1. 完成测评，进入结果页面
2. 验证雷达图是否显示
3. 检查浏览器控制台无错误

### 优势
- 修复简单，只需修改几行代码
- 不影响其他功能
- 提高代码可读性和维护性