### 问题分析
1. **图片导出功能无法正常工作**：
   - html2canvas可能没有正确加载，它是通过html2pdf.bundle.min.js引入的，但可能无法直接访问
   - 在本地file://协议下，html2canvas可能受到浏览器安全限制
   - 元素渲染复杂性高，可能导致html2canvas失败
   - 跨域资源问题可能阻止图片渲染

2. **PDF导出功能正常**：
   - PDF导出使用utils.generatePDF()，已经验证可以正常工作
   - 保留PDF导出功能即可满足用户导出需求

3. **用户要求**：
   - 若图片无法导出，就取消导出图片按钮
   - 优先保证功能稳定，避免用户体验问题

### 修复方案
1. **移除导出图片按钮**：
   - 从HTML中删除export-image-btn按钮元素
   - 调整导出按钮布局，只保留PDF导出按钮

2. **移除相关JavaScript代码**：
   - 删除exportImage函数
   - 删除相关的事件监听器
   - 清理不必要的代码

3. **优化页面布局**：
   - 确保移除图片导出按钮后，PDF导出按钮居中显示
   - 保持页面美观和用户体验

4. **确保PDF导出功能正常**：
   - 验证PDF导出功能不受影响
   - 确保按钮状态正确管理

### 预期效果
- 页面只显示PDF导出按钮，避免用户尝试无法工作的功能
- PDF导出功能正常，用户可以导出测评报告
- 页面布局美观，用户体验良好

### 文件修改
- 修改test.html，移除导出图片按钮和相关JavaScript代码

### 测试步骤
1. 完成测评，进入结果页面
2. 验证只显示PDF导出按钮
3. 测试PDF导出功能是否正常
4. 确认页面布局美观

### 优势
- 简化了页面，减少了用户混淆
- 避免了图片导出功能带来的用户体验问题
- 保留了可靠的PDF导出功能
- 减少了代码复杂性，便于维护